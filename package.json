{
  "name": "i2-automation",
  "version": "1.0.0",
  "description": "Automasi pengujian website Indonesia Indicator menggunakan Playwright",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "report": "npx playwright show-report test-report --host 0.0.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}